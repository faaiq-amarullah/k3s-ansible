---
- name: restart haproxy
  ansible.builtin.systemd:
    name: haproxy
    state: restarted
    daemon_reload: yes

- name: restart keepalived
  ansible.builtin.systemd:
    name: keepalived
    state: restarted
    daemon_reload: yes
